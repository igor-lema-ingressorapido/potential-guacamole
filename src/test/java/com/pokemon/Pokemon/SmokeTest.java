/*package com.pokemon.Pokemon;

import com.pokemon.Pokemon.controller.PokemonController;
import com.pokemon.Pokemon.dao.PokemonDao;
import com.pokemon.Pokemon.service.PokemonService;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import static org.assertj.core.api.Assertions.assertThat;

@RunWith(SpringRunner.class)
@SpringBootTest
public class SmokeTest {

    @Autowired
    private PokemonController pokemonController;

    @Autowired
    private PokemonService pokemonService;

    @Autowired
    private PokemonDao pokemonDao;

    @Test
    public void contextloads() throws Exception {
        assertThat(pokemonController).isNotNull();
        assertThat(pokemonService).isNotNull();
        assertThat(pokemonDao).isNotNull();
    }

}*/
